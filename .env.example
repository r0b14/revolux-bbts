# Copie este arquivo para `.env.local` e preencha com os valores do seu projeto Firebase.
# Não comite `.env.local` em repositórios públicos.

# Valores necessários para inicializar o Firebase (Auth + Firestore)
VITE_FIREBASE_API_KEY=
VITE_FIREBASE_AUTH_DOMAIN=
VITE_FIREBASE_PROJECT_ID=

# URL opcional da Azure Function para análise CSV (se você tiver). Se vazio, o app usará um analisador local para testes.
VITE_AZURE_FN_URL=

# Exemplo rápido de uso:
# 1. crie `.env.local` com estes valores
# 2. reinicie o dev server (`npm run dev`)
